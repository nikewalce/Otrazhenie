# Otrazhenie — TODO

Список задач сгруппирован по областям. Отмеченные `[x]` пункты — сделаны на основе истории коммитов и текущей структуры проекта. Список актуализирован для MVP-стадии.

---

## 1. Настройка проекта

- [x] Создать виртуальное окружение (Poetry) и установить зависимости
- [x] Создать структуру проекта (`app/`, `analyzers/`, `db/`, `templates/`, `static/`, `tests/`)
- [x] Настроить PostgreSQL (база, пользователь, пароль)
- [x] Настроить подключение через SQLAlchemy
- [x] Проверить запуск сервера (`flask run`)
- [x] Настроить CSRF-защиту форм
- [ ] Заполнить `config.py` (конфигурация под dev/prod окружения)
- [ ] Сделать валидацию через Pydantic в `schemas.py` (по всем входным точкам, не только частично)
- [ ] Рефакторинг `app/__init__.py`: явная фабрика приложения (загрузка конфига → подключение БД → регистрация blueprints)
- [ ] Перенести оставшуюся бизнес-логику из `routes/` в `app/services/`
- [ ] Добавить логирование (структурированное, с уровнями: info/warning/error)
- [ ] Добавить docstring-комментарии к функциям (назначение, входные/выходные параметры)

---

## 2. База данных

- [x] Таблица `product_ingredients`
- [x] Таблица `ingredient_categories`
- [x] Таблица `products`
- [x] Модели пользователей (SQLAlchemy)
- [x] Скрипт импорта CSV (`csv_to_db.py`) → PostgreSQL
- [x] Функция `select_one_ingredient(ingredient_name: str)` для поиска ингредиента по имени
- [ ] Расширить `crud.py`: единообразные функции create/read/update/delete для всех моделей
- [ ] Рефакторинг CRUD-слоя под ООП (базовый generic-класс репозитория)
- [ ] Таблица `user_products` (`id`, `user_id`, `product_id`, `scanned_at`) — основа для дневника
- [ ] Таблица `product_recommendations` (`product_id`, `skin_type`, ...) — основа для рекомендаций
- [ ] Таблица `partner_products` (`id`, `name`, `brand`, `link`, `ingredients`) — основа для аналогов
- [ ] Миграции через Alembic (вместо ручного создания таблиц)

---

## 3. Сканер и ввод состава

- [x] Главная страница с формой загрузки штрихкода и формой ручного ввода
- [x] Эндпоинт `/handle-scan`: декодирование штрихкода через pyzbar
- [ ] Очистить языковые префиксы в данных страны (`en:belarus` → `Беларусь` / `Belarus`)
- [ ] Очистить языковые префиксы в категориях (`fr:baume hydratant` → нормальное название без `fr:`)
- [ ] Корректно обрабатывать случай, когда состав продукта не указан во внешних данных (понятный fallback вместо пустого/сломанного вывода)
- [ ] Реализовать `/handle-search` — поиск продукта по названию
- [ ] Реализовать `/manual-analysis` полноценно (сейчас частично работает форма, нужно довести парсинг до уровня `/handle-scan`)
- [ ] Обработка ошибок сканирования: нечитаемое изображение, отсутствие штрихкода — понятное сообщение пользователю с предложением ручного ввода

---

## 4. Анализ ингредиентов

- [x] View-функция `results()` для роута `/results`
- [x] Парсинг состава на ингредиенты с подстановкой данных из БД
- [x] Таблица результатов: название, функция, оценка безопасности, описание
- [x] Цветовая индикация уровня риска
- [x] Автодобавление новой категории ингредиента, если она ещё не известна
- [x] Вынесение функций анализа из `main.py` в отдельные модули
- [ ] Прозрачность источника оценки безопасности (указание, на основе какой методологии/источника посчитан `safety_score`)
- [ ] Дисклеймер на странице результатов: оценка не является медицинской рекомендацией
- [ ] Обработка неизвестных ингредиентов: явный статус «не найден в базе» вместо умалчивания

---

## 5. История сканирований (дневник)

- [ ] Создать таблицу `user_products`
- [ ] Добавлять запись в `user_products` при успешном анализе продукта
- [ ] Страница `/diary` с историей сканирований пользователя
- [ ] Возможность отметить продукт «избранным»
- [ ] Фильтрация дневника по дате и по рейтингу безопасности

---

## 6. Рекомендации по типу кожи

- [ ] Добавить поле `skin_type` в модель пользователя
- [ ] Простая rule-based логика: какие категории ингредиентов нежелательны для какого типа кожи
- [ ] Таблица `product_recommendations`
- [ ] Страница `/recommendations` с базовыми рекомендациями (без ML — на старте достаточно явных правил)
- [ ] Учёт аллергий пользователя (поле уже заявлено в README, проверить наличие в модели) при формировании рекомендаций

---

## 7. Рекомендации безопасных аналогов

- [ ] Таблица `partner_products`
- [ ] Функция подбора аналогов с совпадением состава ≥ 70%
- [ ] Вывод списка альтернатив со ссылками на магазины/источники
- [ ] (Опционально, после MVP) интеграция с партнёрским API вместо собственной базы

---

## 8. Тестирование

- [ ] Юнит-тесты:
  - [ ] `decode_barcode` (включая edge cases: нет штрихкода, повреждённое изображение)
  - [ ] `select_one_ingredient` / функции поиска ингредиента
  - [ ] Парсинг состава в `manual_analysis` (смешанные языки, неизвестные ингредиенты, пустая строка)
- [ ] Интеграционные тесты:
  - [ ] POST `/handle-scan`, `/handle-search`, `/manual-analysis`
  - [ ] GET `/results`, `/diary`, `/recommendations`
- [ ] Тесты на обработку ошибок:
  - [ ] Файл не передан
  - [ ] Пустой состав
  - [ ] Продукт не найден в базе
- [ ] Настроить CI (запуск тестов на каждый push/PR)

---

## 9. UI/UX

- [ ] Проверка адаптивности на мобильных устройствах (приоритет — сканирование делают чаще с телефона)
- [ ] Flash-сообщения о статусе операций (успех/ошибка)
- [ ] Индикатор прогресса анализа/загрузки изображения
- [ ] Понятные состояния пустых данных (нет истории, нет рекомендаций и т.д.)

---

## 10. Продакшн и инфраструктура

- [x] Dockerfile для контейнеризации приложения
- [ ] Настроить Gunicorn + Nginx
- [ ] Перенести БД на удалённый сервер PostgreSQL (managed-решение или отдельный сервер)
- [ ] Настроить `.env` для секретов и ключей, исключить из git (проверить `.gitignore`)
- [ ] Логирование ошибок и действий пользователей в продакшне
- [ ] Бэкапы базы данных

---

## 11. Документация

- [ ] Описать процесс настройки `.env` (пример `.env.example`)
- [ ] Документация по API-эндпоинтам (когда появится отдельный REST-слой для мобильного клиента)